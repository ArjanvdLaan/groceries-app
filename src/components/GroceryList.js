import React from "react"
import List from "./List"

function GroceryList(key, title, clickItem) {
   console.log(key, title, clickItem)
    return(
        <div>
            <List
                
            />
        </div>
   ) 
}

export default GroceryList