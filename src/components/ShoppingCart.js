import React from "react"
import List from "./List"


function ShoppingCart(key, title, clickItem) {
    console.log(key, title, clickItem)
    return(
         <div>
             <List/>
         </div>
    ) 
 }

export default ShoppingCart